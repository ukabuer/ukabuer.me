import { style as s } from "@vanilla-extract/css";
import * as g from "../../components/Layout/global.css.js";

export const banner = s([
	g.banner,
	{
		backgroundColor: "#751816",
	},
]);

export const section = s([
	g.section,
	{
		maxWidth: 600,
	},
]);

export default "";
